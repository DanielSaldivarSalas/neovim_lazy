--[[local options = {
	--line numbers
	number = true,
	relativenumber = true,
	autowrite = true,
}
]]

local opt = vim.opt
opt.number = true
opt.number = true
--vim.opt.autowrite = true

--for k, v in pairs(options) do
--	vim.opt[k] = v
--end
